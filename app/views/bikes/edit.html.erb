<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for @bike do |f| %>
		<%= f.text_field :name %>
		<%= f.text_area :description %>
		<%= f.submit "Save Bike", :class => "btn btn-primary" %>
		<% end %>
	</div>
</div>